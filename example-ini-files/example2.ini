This is a garbage value, however, it won't break the INI parser when loaded.

Key=Value ; This key cannot be accessed by the INI parser because it isn't located under a section.

[App]
Hello=World

This is another garbage value.